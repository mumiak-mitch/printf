#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <limits.h>
#include <unistd.h>

int _printf(const char *format, ...);
int binary_length(unsigned int num);
void print_binary(unsigned int num);

#endif /* MAIN_H */

